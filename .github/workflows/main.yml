name: My Action with Email

on: 
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: 23f2003496@ds.study.iitm.ac.in
        run: echo "Hello, world!"
