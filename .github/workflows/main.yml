name: learn-github-actions
run-name: ${{ github.actor }} is learning GitHub Actions
on: [commit]
jobs:
  test:
  runs-on: "Ubuntu 24.04.3 LTS"
    steps: 
      name: 23f2003496@ds.study.iitm.ac.in
      run: echo "Hello, world!"
